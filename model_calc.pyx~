# This file is designed to model a simplified version of CTH's mesh calculation
# This is to see if the calculation can be improved

import numpy as np

from mesh import *


class initCond:
    def __init__(self, fName):
        try:
            # TODO: read fName
            exit()
        except:
            # set default values
            self.meshShape = (64, 64, 64)


def model_calc(initCond, tmax):
    # set up the mesh
    mesh = Mesh(initCond.meshShape)
    
    # initialization:
    # two squares in orbit
    
    
    # integrate
    
