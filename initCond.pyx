import numpy as np




class initCond:
    def __init__(self, fName):
        try:
            # TODO: read fName
            exit()
        except:
            # set default values
            self.meshShape = (64, 64, 64)
